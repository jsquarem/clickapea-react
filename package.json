{
  "name": "clickapea",
  "version": "0.1.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd app/ && npm install && npm run build && cd server/ & npm install"
  },
  "license": "MIT"
}
