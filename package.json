{
  "name": "clickapea",
  "version": "0.1.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd server && npm install && npm start",
    "heroku-postbuild": "cd app/ && npm install && npm run build"
  },
  "license": "MIT"
}
