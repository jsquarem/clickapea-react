{
  "name": "clickapea",
  "version": "0.1.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start-server": "cd server && node server.js",
    "build": "cd app/ && npm install && npm run build && npm start && cd .. && npm start-server"
  },
  "cacheDirectories": [
    "node_modules",
    "app/node_modules",
    "server/node_modules"
  ],
  "license": "MIT",
  "devDependencies": {}
}
