{
  "name": "clickapea-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.1223.0",
    "bcrypt": "^5.0.1",
    "dayjs": "^1.11.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "immutability-helper": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.6.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.2.10",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "@jest-mock/express": "^2.0.1",
    "jest": "^29.1.2"
  },
  "scripts": {
    "server:test": "npx jest",
    "start": "node server.js",
    "build:ui": "cd ../app && npm install && npm run build",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:3001"
}
